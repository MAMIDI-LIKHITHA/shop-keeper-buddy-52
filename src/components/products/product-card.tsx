import { Link } from "@tanstack/react-router";
import { ArrowUpRight } from "lucide-react";
import { formatPrice, placeholderImage, type Product, whatsappHref } from "@/lib/catalog";

export function ProductCard({ product, imageUrl, whatsappNumber }: { product: Product; imageUrl: string | undefined; whatsappNumber: string }) {
  return <article className="group"><Link to="/products/$productId" params={{ productId: product.id }} className="block"><div className="relative aspect-[4/5] overflow-hidden rounded-2xl bg-muted"><img src={imageUrl || product.image_url || placeholderImage} alt={product.name} className="h-full w-full object-cover transition-transform duration-700 group-hover:scale-105" /><span className="absolute left-4 top-4 rounded-full bg-background/90 px-3 py-1 text-[10px] font-bold uppercase tracking-[0.18em] text-foreground">{product.category}</span><span className="absolute bottom-4 right-4 grid h-10 w-10 place-items-center rounded-full bg-background text-foreground opacity-0 shadow-lg transition-opacity group-hover:opacity-100"><ArrowUpRight size={17} /></span></div></Link><div className="mt-4 flex items-start justify-between gap-4"><div><h3 className="font-display text-xl font-semibold">{product.name}</h3><p className="mt-1 line-clamp-2 text-sm leading-6 text-muted-foreground">{product.description}</p></div><p className="shrink-0 text-sm font-semibold">{formatPrice(product.price)}</p></div><div className="mt-4 flex items-center justify-between"><span className={product.available ? "text-xs font-semibold text-primary" : "text-xs font-semibold text-muted-foreground"}>{product.available ? "Available" : "Currently Unavailable"}</span><a href={whatsappHref(whatsappNumber, product.name)} target="_blank" rel="noreferrer" className="text-xs font-semibold underline-offset-4 hover:underline">Contact</a></div></article>;
}